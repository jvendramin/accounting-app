import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

// TODO: port from /Users/jacobvendramin/accounting-app/frontend/src/pages/.
// API route already implemented at /api/reports/balance-sheet.
// Use Intent UI components: Table, Modal, TextField, Select, DatePicker.

export default function Page() {
  return (
    <Card>
      <CardHeader><CardTitle>reports/balance-sheet</CardTitle></CardHeader>
      <CardContent className="text-muted-fg text-sm">Port me.</CardContent>
    </Card>
  )
}
